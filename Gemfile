# frozen_string_literal: true
source 'https://rubygems.org'

# automation
gem 'rake'

# web interface
gem 'sinatra'
gem 'sinatra-contrib'

gem 'faker'
gem 'activesupport'

group :development do
  gem 'shotgun'
end

group :test do
  gem 'minitest'
  gem 'minitest-hooks'
  gem 'rack-test'
end
